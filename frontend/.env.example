# URL of the deployed FastAPI backend, without a trailing slash.
VITE_API_BASE_URL=http://localhost:8000# Copy this file to .env and adjust if the backend runs elsewhere.
VITE_API_BASE_URL=http://localhost:8000
